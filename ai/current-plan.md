# 当前计划

更新时间: 2026-07-08

## 当前阶段

当前处于 M0: 仓库统一与基线确认阶段。

## 已确定基线

- 后端使用 `RuoYi-Vue` 的 `springboot3` 分支。
- Web 管理端使用 `RuoYi-Vue3` 的 `typescript` 分支。
- 小程序/移动端使用 `RuoYi-App` 的 `vue3` 分支。
- 根仓库目标地址: `https://github.com/Wangdongzhi756/dalian_sea_ai.git`。

## 本阶段任务

- [x] 确认三端代码目录和对应分支。
- [x] 建立 `ai` 项目文档目录。
- [x] 编写项目计划、当前计划、长期计划和项目 spec。
- [x] 编写根目录 `agents.md`。
- [x] 清理三端原有 `.git` 元数据。
- [x] 初始化根目录 Git 仓库并配置 GitHub 远端。
- [ ] 提交三端代码和项目文档。
- [ ] 推送到 GitHub 目标仓库。

## 下一阶段建议

- 编写统一启动文档，明确后端、Web 管理端、小程序的启动命令和依赖版本。
- 将项目名称、接口地址、应用名称从若依默认值逐步替换为大连海 AI SaaS。
- 建立业务模块清单和数据库设计草案。
