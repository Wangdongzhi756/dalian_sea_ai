# 当前计划

更新时间: 2026-07-08

## 当前阶段

已按要求回退到 M1 项目准备与脚手架清理完成后的空骨架状态。当前代码只保留三端基础工程、若伊用户/组织/登录/权限/安全/日志等基础能力，以及 Docker 本地 MySQL/Redis 启动环境；海事业务、AI 文案、推流、船长端等功能代码已回退，后续 M2 功能开发需要重新确认方案后再开始。

## 已确定基线

- 后端目录调整为 `backend`，目标包名 `com.daliansea.ai`。
- Web 管理端目录调整为 `admin-web`。
- 小程序/移动端目录调整为 `mobile-app`。
- 根仓库目标地址: `https://github.com/Wangdongzhi756/dalian_sea_ai.git`。

## 本阶段任务

- [x] 确认三端代码目录和对应分支。
- [x] 建立 `ai` 项目文档目录。
- [x] 编写项目计划、当前计划和项目 spec。
- [x] 编写根目录 `agents.md`。
- [x] 清理三端原有 `.git` 元数据。
- [x] 初始化根目录 Git 仓库并配置 GitHub 远端。
- [x] 提交三端代码和项目文档。
- [x] 推送到 GitHub 目标仓库。
- [x] 删除泛化长期计划，合并为可执行项目计划。
- [x] 重命名三端目录，去掉默认脚手架目录名。
- [x] 替换后端 Maven 坐标和 Java 包名。
- [x] 删除代码生成和定时任务等非核心模块。
- [x] 清理前端/移动端默认品牌、菜单、标题和接口地址。
- [x] 验证后端编译和前端类型/构建入口。

## 当前开发计划

- [ ] M2-0: 重新确认产品方案、核心对象、权限边界和数据库设计。
- [ ] M2-1: 确认后再扩展租户/船东/船长/船舶等基础模型。
- [ ] M2-2: 确认后再开发 AI 文案、公众号、推流和船长端功能。
- [ ] M2-3: 确认后再进入业务流程、审核、通知和运营后台开发。

## 回退验收口径

- 保留三端目录: `backend`、`admin-web`、`mobile-app`。
- 保留若伊核心能力: 用户、组织、角色、菜单、登录、权限、安全、日志、字典、参数。
- 保留 Docker 本地环境: MySQL、Redis、初始化 SQL。
- 不保留已开发功能: 海事业务 CRUD、AI 知识库、AI 文案模板、内容发布、渠道配置、推流回执、船长端页面、租户隔离过滤器。
- 当前版本作为可启动空骨架，后续功能必须先明确方案再开发。
