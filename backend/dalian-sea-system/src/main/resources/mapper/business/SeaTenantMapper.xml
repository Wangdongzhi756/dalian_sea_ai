<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.daliansea.ai.system.mapper.SeaTenantMapper">

    <resultMap type="SeaTenant" id="SeaTenantResult">
        <id     property="tenantId"     column="tenant_id"     />
        <result property="tenantCode"   column="tenant_code"   />
        <result property="tenantName"   column="tenant_name"   />
        <result property="contactName"  column="contact_name"  />
        <result property="contactPhone" column="contact_phone" />
        <result property="contactEmail" column="contact_email" />
        <result property="planType"     column="plan_type"     />
        <result property="maxVessels"   column="max_vessels"   />
        <result property="expireTime"   column="expire_time"   />
        <result property="status"       column="status"        />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"   column="create_time"   />
        <result property="updateBy"     column="update_by"     />
        <result property="updateTime"   column="update_time"   />
        <result property="remark"       column="remark"        />
    </resultMap>

    <sql id="selectTenantVo">
        select tenant_id, tenant_code, tenant_name, contact_name, contact_phone, contact_email, plan_type,
               max_vessels, expire_time, status, create_by, create_time, update_by, update_time, remark
        from sea_tenant
    </sql>

    <select id="selectTenantList" parameterType="SeaTenant" resultMap="SeaTenantResult">
        <include refid="selectTenantVo"/>
        <where>
            <if test="tenantCode != null and tenantCode != ''">
                AND tenant_code like concat('%', #{tenantCode}, '%')
            </if>
            <if test="tenantName != null and tenantName != ''">
                AND tenant_name like concat('%', #{tenantName}, '%')
            </if>
            <if test="contactPhone != null and contactPhone != ''">
                AND contact_phone like concat('%', #{contactPhone}, '%')
            </if>
            <if test="planType != null and planType != ''">
                AND plan_type = #{planType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by tenant_id desc
    </select>

    <select id="selectTenantOptions" resultMap="SeaTenantResult">
        <include refid="selectTenantVo"/>
        where status = '0'
        order by tenant_name asc
    </select>

    <select id="selectTenantById" parameterType="Long" resultMap="SeaTenantResult">
        <include refid="selectTenantVo"/>
        where tenant_id = #{tenantId}
    </select>

    <select id="checkTenantCodeUnique" parameterType="String" resultMap="SeaTenantResult">
        <include refid="selectTenantVo"/>
        where tenant_code = #{tenantCode} limit 1
    </select>

    <insert id="insertTenant" parameterType="SeaTenant" useGeneratedKeys="true" keyProperty="tenantId">
        insert into sea_tenant(
            <if test="tenantCode != null and tenantCode != ''">tenant_code,</if>
            <if test="tenantName != null and tenantName != ''">tenant_name,</if>
            <if test="contactName != null and contactName != ''">contact_name,</if>
            <if test="contactPhone != null and contactPhone != ''">contact_phone,</if>
            <if test="contactEmail != null and contactEmail != ''">contact_email,</if>
            <if test="planType != null and planType != ''">plan_type,</if>
            <if test="maxVessels != null">max_vessels,</if>
            <if test="expireTime != null">expire_time,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="tenantCode != null and tenantCode != ''">#{tenantCode},</if>
            <if test="tenantName != null and tenantName != ''">#{tenantName},</if>
            <if test="contactName != null and contactName != ''">#{contactName},</if>
            <if test="contactPhone != null and contactPhone != ''">#{contactPhone},</if>
            <if test="contactEmail != null and contactEmail != ''">#{contactEmail},</if>
            <if test="planType != null and planType != ''">#{planType},</if>
            <if test="maxVessels != null">#{maxVessels},</if>
            <if test="expireTime != null">#{expireTime},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>

    <update id="updateTenant" parameterType="SeaTenant">
        update sea_tenant
        <set>
            <if test="tenantCode != null and tenantCode != ''">tenant_code = #{tenantCode},</if>
            <if test="tenantName != null and tenantName != ''">tenant_name = #{tenantName},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="contactEmail != null">contact_email = #{contactEmail},</if>
            <if test="planType != null and planType != ''">plan_type = #{planType},</if>
            <if test="maxVessels != null">max_vessels = #{maxVessels},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where tenant_id = #{tenantId}
    </update>

    <delete id="deleteTenantById" parameterType="Long">
        delete from sea_tenant where tenant_id = #{tenantId}
    </delete>

    <delete id="deleteTenantByIds" parameterType="Long">
        delete from sea_tenant where tenant_id in
        <foreach collection="array" item="tenantId" open="(" separator="," close=")">
            #{tenantId}
        </foreach>
    </delete>

</mapper>
