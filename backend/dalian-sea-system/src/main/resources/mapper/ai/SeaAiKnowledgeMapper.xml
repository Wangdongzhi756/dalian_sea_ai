<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.daliansea.ai.system.mapper.SeaAiKnowledgeMapper">

    <resultMap type="SeaAiKnowledge" id="SeaAiKnowledgeResult">
        <id     property="knowledgeId" column="knowledge_id" />
        <result property="tenantId"    column="tenant_id"    />
        <result property="tenantName"  column="tenant_name"  />
        <result property="title"       column="title"        />
        <result property="category"    column="category"     />
        <result property="sourceType"  column="source_type"  />
        <result property="content"     column="content"      />
        <result property="tags"        column="tags"         />
        <result property="status"      column="status"       />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"  column="create_time"  />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"  column="update_time"  />
        <result property="remark"      column="remark"       />
    </resultMap>

    <sql id="selectKnowledgeVo">
        select k.knowledge_id, k.tenant_id, t.tenant_name, k.title, k.category, k.source_type,
               k.content, k.tags, k.status, k.create_by, k.create_time, k.update_by,
               k.update_time, k.remark
        from sea_ai_knowledge k
        left join sea_tenant t on k.tenant_id = t.tenant_id
    </sql>

    <select id="selectKnowledgeList" parameterType="SeaAiKnowledge" resultMap="SeaAiKnowledgeResult">
        <include refid="selectKnowledgeVo"/>
        <where>
            <if test="tenantId != null">
                AND k.tenant_id = #{tenantId}
            </if>
            <if test="title != null and title != ''">
                AND k.title like concat('%', #{title}, '%')
            </if>
            <if test="category != null and category != ''">
                AND k.category = #{category}
            </if>
            <if test="sourceType != null and sourceType != ''">
                AND k.source_type = #{sourceType}
            </if>
            <if test="tags != null and tags != ''">
                AND k.tags like concat('%', #{tags}, '%')
            </if>
            <if test="status != null and status != ''">
                AND k.status = #{status}
            </if>
        </where>
        order by k.knowledge_id desc
    </select>

    <select id="selectKnowledgeById" parameterType="Long" resultMap="SeaAiKnowledgeResult">
        <include refid="selectKnowledgeVo"/>
        where k.knowledge_id = #{knowledgeId}
    </select>

    <insert id="insertKnowledge" parameterType="SeaAiKnowledge" useGeneratedKeys="true" keyProperty="knowledgeId">
        insert into sea_ai_knowledge(
            <if test="tenantId != null">tenant_id,</if>
            <if test="title != null and title != ''">title,</if>
            <if test="category != null and category != ''">category,</if>
            <if test="sourceType != null and sourceType != ''">source_type,</if>
            <if test="content != null and content != ''">content,</if>
            <if test="tags != null">tags,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="tenantId != null">#{tenantId},</if>
            <if test="title != null and title != ''">#{title},</if>
            <if test="category != null and category != ''">#{category},</if>
            <if test="sourceType != null and sourceType != ''">#{sourceType},</if>
            <if test="content != null and content != ''">#{content},</if>
            <if test="tags != null">#{tags},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>

    <update id="updateKnowledge" parameterType="SeaAiKnowledge">
        update sea_ai_knowledge
        <set>
            <if test="tenantId != null">tenant_id = #{tenantId},</if>
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="category != null and category != ''">category = #{category},</if>
            <if test="sourceType != null and sourceType != ''">source_type = #{sourceType},</if>
            <if test="content != null and content != ''">content = #{content},</if>
            <if test="tags != null">tags = #{tags},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where knowledge_id = #{knowledgeId}
    </update>

    <delete id="deleteKnowledgeByIds" parameterType="Long">
        delete from sea_ai_knowledge where knowledge_id in
        <foreach collection="array" item="knowledgeId" open="(" separator="," close=")">
            #{knowledgeId}
        </foreach>
    </delete>

</mapper>
