# 大连海 AI SaaS 管理端

管理端基于 Vue 3、TypeScript、Vite、Element Plus、Pinia 和 Vue Router。

当前保留登录、权限菜单、系统管理、日志和基础后台布局；已删除代码生成、表单构建、定时任务、Druid 和 Swagger 页面。

## 命令

```bash
npm install
npm run dev
npm run build:prod
```
