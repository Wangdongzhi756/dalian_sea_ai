# 大连海 AI SaaS 移动端

移动端基于 uni-app + Vue 3，用于业务提交、进度查看、消息通知和后续移动 AI 助手。

当前保留登录、我的、工作台、资料维护、头像修改、密码修改、常见问题和关于页面。

## 配置

接口地址位于 `config.js` 的 `baseUrl`，默认指向本地后端：

```js
baseUrl: 'http://localhost:8080'
```
